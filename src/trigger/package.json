{
  "name": "etrm-factory-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "Linear webhook handler: verifies signature, filters for the agent-ready label, and invokes the AgentCore pipeline runtime.",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.1115.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0"
  }
}
